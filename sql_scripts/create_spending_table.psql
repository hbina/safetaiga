CREATE TABLE IF NOT EXISTS yaapos_spending(spendingId SERIAL, userId INTEGER, spendingName TEXT NOT NULL, spendingPrice DECIMAL, spendingDescription TEXT, spendingUnixTime BIGINT NOT NULL, PRIMARY KEY (spendingId), FOREIGN KEY (userId) REFERENCES yaapos_user(userId));
TRUNCATE yaapos_spending;